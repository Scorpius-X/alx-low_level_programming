#include "main.h"
/**
 * print_last_digit - prints the last digit
 *
 * @n: integer
 *
 * Return: returns last digit n % 10
 */

int print_last_digit(int n)
{
	int last_digit = n % 10;
	return (last_digit);
}
