#include "main.h"
/**
 * _abs - calculates the absolute value
 *
 * Return: 0 success
 */

int _abs(int)
{
	int x;
	int y = (x * (-1);

			if (x < 0)
			{
			return (y);
			}
			else
			{
			return (x);
			}
			}
